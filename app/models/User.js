const path = require("path");
const config = require(path.join(__dirname, "../", "config"));

function getPlayers(search, gender, sport, req) {
    return new Promise((resolve, reject) => {});
}

module.exports = {};
